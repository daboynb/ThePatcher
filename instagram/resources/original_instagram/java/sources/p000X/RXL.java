package p000X;

/* loaded from: classes13.dex */
public abstract class RXL {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 0 ? intValue != 1 ? intValue != 4 ? "background and dnd" : "foreground and dnd" : "not supported" : "not logged in";
    }
}
