package p000X;

/* loaded from: classes15.dex */
public abstract class X2A {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 1 ? intValue != 2 ? intValue != 3 ? intValue != 4 ? intValue != 5 ? "STORIES" : "UNKNOWN" : "HORIZON_XSCREEN_TO_DIRECT" : "DIRECT" : "FEED" : "REELS";
    }
}
