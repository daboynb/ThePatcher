package p000X;

/* loaded from: classes13.dex */
public abstract class RMF {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 1 ? intValue != 2 ? intValue != 3 ? intValue != 4 ? "control_action" : "tab_source" : "reels_watch_time" : "stack_name" : "load_time_ms";
    }
}
