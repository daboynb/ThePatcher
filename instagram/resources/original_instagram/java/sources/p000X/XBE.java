package p000X;

/* loaded from: classes15.dex */
public abstract class XBE {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 0 ? intValue != 1 ? intValue != 3 ? intValue != 4 ? "collection_tile_product" : "shopping_pdp_button" : "pill_button" : "shopping_product_tags" : "product_tag";
    }
}
