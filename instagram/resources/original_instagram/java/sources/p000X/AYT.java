package p000X;

/* loaded from: classes6.dex */
public abstract class AYT {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 1 ? intValue != 2 ? intValue != 3 ? intValue != 4 ? intValue != 5 ? "INBOX" : "SUPPORT_INBOX" : "LOCKED_CHAT" : "BC_PARTNERSHIP_INBOX" : "SPAM_INBOX" : "PENDING_INBOX";
    }
}
