package p000X;

/* loaded from: classes10.dex */
public abstract class MHC {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 0 ? intValue != 2 ? intValue != 3 ? intValue != 4 ? "suggested_businesses" : "discover_people_self_profile_entry" : "nux_based_on_interests" : "nux_explore_people" : "discover_people";
    }
}
