package p000X;

/* loaded from: classes12.dex */
public abstract class PHU {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 1 ? intValue != 2 ? intValue != 3 ? intValue != 4 ? "Response data is null" : "Exception parsing autofill applied statuses call" : "WebView is null" : "Dialog fragment is present and not removing" : "Bottomsheet is null";
    }
}
