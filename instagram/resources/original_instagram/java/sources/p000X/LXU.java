package p000X;

/* loaded from: classes10.dex */
public abstract class LXU {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 0 ? intValue != 1 ? intValue != 2 ? "registration_reminder_deeplink" : "identity_switcher" : "password_saving" : "land_on_existing_feed";
    }
}
