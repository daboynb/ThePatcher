package p000X;

/* loaded from: classes12.dex */
public abstract class PHV {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 1 ? intValue != 2 ? intValue != 3 ? intValue != 4 ? "Missing data" : "Unable to parse json" : "Webview is null" : "Payment or Hybrid Save Prompt is still present" : "Missing Component";
    }
}
