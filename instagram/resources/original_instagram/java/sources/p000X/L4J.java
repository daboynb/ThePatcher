package p000X;

/* loaded from: classes10.dex */
public abstract class L4J {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 0 ? intValue != 1 ? intValue != 3 ? intValue != 4 ? intValue != 5 ? "push" : "notification" : "comment" : "reel_overflow_menu" : "reel" : "ufi";
    }
}
