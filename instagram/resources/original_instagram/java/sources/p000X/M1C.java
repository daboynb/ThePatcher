package p000X;

/* loaded from: classes10.dex */
public abstract class M1C {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 1 ? intValue != 2 ? intValue != 3 ? intValue != 4 ? intValue != 5 ? "cancel" : "logout" : "skip" : "next" : "link_click" : "dismiss";
    }
}
