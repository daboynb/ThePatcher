package p000X;

/* loaded from: classes10.dex */
public abstract class M5H {
    public static String A00(Integer num) {
        int intValue = num.intValue();
        return intValue != 0 ? intValue != 2 ? intValue != 3 ? intValue != 4 ? "none" : "bloks_extension" : "url" : "setting" : "qp";
    }
}
